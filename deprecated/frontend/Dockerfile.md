# Deprecated frontend Dockerfile

Original path: `frontend/Dockerfile`

This Docker-specific frontend build file was removed from the active app path because Version 1 is Windows local-first and does not require Docker for normal operation.

The original file content remains recoverable from Git history before this cleanup.
