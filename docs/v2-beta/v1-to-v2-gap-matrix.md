# V1 to V2 Gap Matrix

Date: 2026-07-09

Purpose: recovery planning for the active `codex/v2-beta-local-rebuild` V2 beta. This matrix was produced before product code changes. It compares the active V2 runtime against the archived V1 runtime under `deprecated/v1/` and treats a capability as complete only when backend, frontend, persistence, validation, security, and tests prove it.

Important: live Alleva credentials were provided out-of-band for testing, but credentials are not copied into this document and must not be committed, logged, exported, or shown in browser payloads.

Checkpoint update: after the initial matrix was written, V2 gained a protected normalized treatment-plan aggregate JSON import path backed by encrypted local SQLite persistence and frontend Manual Upload wiring. V2 also gained protected UTF-8 text, Markdown, CSV, TSV, PDF, and XLSX manual treatment-plan parsing into the same encrypted aggregate/checklist path, plus encrypted original source-file archival for those uploads without storing original filenames. Archived manual source files now appear as safe metadata in Treatment Plan detail and can be downloaded or deleted by manager/admin users through role-gated, audited, path-safe routes that use generated filenames and preserve the normalized aggregate. Durable manager-action storage for approve/comment/return/override actions is in place, with persisted reviews merged into Treatment Plan detail and shown in the UI after reload/restart. Checklist evidence CSV export and API harness artifact downloads now have bounded, tested safety checks. Patient correction UI, full return-for-correction workflow, and live Alleva sync remain open gaps.

## Evidence Read

- Active V2 backend: `backend/app/main.py`, `backend/app/desktop_main.py`, `backend/app/v2/api/routes.py`, `backend/app/v2/api/models.py`, `backend/app/v2/domain/schemas.py`, `backend/app/v2/services/*`, `backend/tests/test_v2_runtime.py`
- Active V2 frontend: `frontend/src/v2/*`, `frontend/src/App.test.tsx`
- V1 archive: `deprecated/v1/backend/app/api/*`, `deprecated/v1/backend/app/models/models.py`, `deprecated/v1/backend/app/schemas/schemas.py`, `deprecated/v1/backend/app/services/*`, `deprecated/v1/frontend/src/App.tsx`, `deprecated/v1/frontend/src/components/*`, `deprecated/v1/tests/backend/*`
- Shared rules/docs: `config/checklists/treatment-plan-v1.json`, `config/rules/alleva_treatment_plan_completeness_rules.yaml`, `docs/patient-treatment-plan-handling.md`, `docs/v2-beta/*`, `README.md`

## Gap Matrix

| Capability | V1 implementation files/routes/models | Current V2 implementation status | Missing pieces | Security/HIPAA risk | Required V2 implementation plan | Acceptance test that proves it works |
|---|---|---|---|---|---|---|
| Persistent storage, schema bootstrap, migrations | `deprecated/v1/backend/app/db/session.py`, `deprecated/v1/backend/app/db/bootstrap.py`, `deprecated/v1/backend/app/models/models.py`; tests `deprecated/v1/tests/backend/test_schema_bootstrap.py` | No active V2 DB/session layer. `backend/app/main.py` only creates app-data folders. `backend/migrations/*.sql` are legacy-style SQL files not wired to V2 runtime startup. | SQLite engine, session dependencies, schema creation/upgrade, local app-data DB path, migration/bootstrap for all product tables. | Without persistence, sessions, users, settings, reviews, jobs, uploads, and audit state can disappear or be faked in memory. | Reintroduce a V2 DB package using SQLite by default under `Settings.local_app_data_dir`; port needed V1 models into V2-owned models; run idempotent bootstrap on startup; keep runtime data outside repo. | Start app with temp `IZ_CNA_LOCAL_APP_DATA_DIR`, create settings/user/review/job records, restart app, verify records still exist and DB file is outside repo. |
| Authentication and session handling | `deprecated/v1/backend/app/api/auth_user_routes.py` `/auth/login`, `/users/me`; `deprecated/v1/backend/app/core/security.py`; `deprecated/v1/backend/app/api/deps.py`; tests `test_auth_flow.py` | `backend/app/v2/api/routes.py` accepts username `admin`, ignores password, returns hardcoded `v2-local-admin-token`; `/api/users/me` always returns static admin. Frontend `AppV2.tsx` sets `loggedIn=true` without calling backend. | Password verification, token creation/verification, session persistence, logout, stale-token clearing, frontend login/profile verification. | Critical. Anyone can log in as admin; browser auth state is fake. | Port V1 password hashing/JWT dependency pattern into active V2; require bearer token for protected routes; make frontend login call `/api/auth/login`, store verified token, call `/api/users/me`, clear invalid sessions. | Backend: wrong password returns 401, correct password returns token, `/api/users/me` without/with bad token returns 401, with good token returns user. Frontend: wrong password shows error; reload with valid token restores session without calling login. |
| User management | V1 `/users`, `/users/{id}`, `/users/{id}/reset-password`, self-profile routes in `auth_user_routes.py`; `User` model; tests `test_user_management.py` | V2 has no user list/create/update/reset/delete routes. `frontend/src/v2/pages/UsersPage.tsx` is a static role table. | User CRUD, deactivate/lock/unlock, admin reset, self profile/password, last login, safeguards for bootstrap/admin users, UI forms. | Critical. Real staff roles cannot be managed; static UI may mislead management into believing accounts are controlled. | Implement V2 user service/routes from V1 behavior; role-aware frontend Users page; audit all user-management changes. | Admin can create manager/counselor, update role/status, reset password, deactivate unused user; manager cannot manage admin; all actions persist and audit. |
| Role-based access control | `deprecated/v1/backend/app/api/deps.py` `get_current_user`, `require_roles`; route dependencies across V1; tests `test_user_management.py`, `test_treatment_plan_timeliness.py` | No V2 route depends on auth or role checks. Navigation endpoint always lists admin screens. | Route-level dependencies, role matrix, non-admin navigation filtering, authorization failure audits. | Critical. Admin-only settings, API config, logs, exports, and user management are exposed. | Define V2 roles (`admin`, `office_manager` or `manager`, `counselor`, `viewer`) and route permissions; return 401/403; filter UI navigation from `/api/v2/navigation`. | Non-admin token gets 403 from admin routes and cannot see Settings/Users/API config nav; failed authorization is audit logged. |
| Password reset/change/lockout | V1 `/auth/reset-password`, `/users/me/change-password`, admin reset, failed login tracking/lockout in `auth_user_routes.py`; tests `test_auth_flow.py`, `test_user_management.py` | V2 has no password reset/change routes and no failed-login counters. | Password policy, must-reset flow, change password, admin reset, lockout, bootstrap admin recovery behavior. | Critical. Weak or default credentials cannot be controlled, and brute-force attempts are not throttled. | Port V1 password policy and lockout behavior; enforce must-reset before workspace; log success/failure without password contents. | Five bad attempts lock account; locked user cannot login; admin reset clears lock and sets must-reset; password change requires current password and rejects weak/new username-derived values. |
| App settings | V1 `/settings` routes in `deprecated/v1/backend/app/api/routes.py`; `AppSetting` model; `app_settings.py`; tests `test_user_management.py`, `test_treatment_plan_timeliness.py` | V2 Settings page is static. Backend has no `/api/settings` route and no persisted `AppSetting`. | Settings API/UI, validation, persistence, timezone, LLM disabled default, LOC/treatment-plan windows, sync limits, raw sensitive mode, readiness fields. | High. Operators cannot configure or verify core behavior through UI; docs overclaim settings exist. | Port V1 settings payload into V2 with narrower V2 labels; redact secret fields; validate ranges; show saved state in Settings UI. | Save settings, restart app, reload settings page/API, verify values persist and secret fields are only configured-state flags. |
| API credential configuration | V1 `/api-configuration` GET/PATCH, test, test-operation, alleva-quick-pull in `api_config_routes.py`; HTML fallback in `api_config_ui_routes.py`; tests `test_api_connectivity.py` | V2 `PATCH /api/api-configuration` returns redacted echo only; not persisted, no GET, only `api_key` field, no token URL/client secret/scopes/pagination/sync limits. | Full API config shape: base URL, token URL, client ID, client secret, auth style, scopes, OpenAPI URL, timeouts, pagination and sync limits, GET/PATCH, redacted UI. | Critical. Supplied real credentials would be lost or mishandled; incomplete config prevents safe Alleva testing. | Build V2 `api_profiles`/settings persistence using encrypted secret storage; UI form supports all required fields and shows redacted configured state only. | Save full Alleva config, inspect DB value is encrypted/non-plaintext, GET shows flags only, frontend reload shows non-secret values and configured flags. |
| Encrypted secret storage | V1 `secure_storage.py` encrypts files and text secrets; used by `patient_notes.py`, `api_config_routes.py`, `api_monitor.py`, `llm_assist.py`, `alleva_treatment_plan_sync.py`; tests in security/API/upload suites | V2 has no secure-storage service. Current API config does not persist secrets. | Fernet/envelope or equivalent, private directory permissions, text-secret helpers, encrypted uploads, encrypted API secrets. | Critical. Plaintext API secrets or PHI files would violate repo security rules and local privacy expectations. | Port V1 secure-storage envelope or improved local key handling; wire secrets and uploads through it; never include plaintext in audits, logs, responses, exports, or docs. | DB/file bytes do not contain saved API secret or uploaded plaintext; decrypt path works through service; audit/export/browser payloads contain no secret substrings. |
| EMR/Alleva profiles | V1 `/emr/profile`, `/emr/profiles`, activate/delete routes in `routes.py`; `EmrEndpointProfile` model; tests `test_system_and_emr_readiness.py` | V2 has no EMR profile routes or UI. API config supports one unsaved echo only. | Profile CRUD, active/default profile, adapter key, URLs, token auth style, encrypted secret, activation, deletion safeguards. | High. Cannot manage real Alleva tenant readiness or separate test/production profiles safely. | Implement V2 EMR profile model/routes/UI; default profile feeds API config and sync/harness jobs; audit changes. | Admin creates two profiles, activates one, restart preserves active profile, non-admin cannot edit, secrets remain encrypted. |
| OpenAPI pull/parse/test operation | V1 `api_connectivity.py` candidate URLs, Swagger UI parsing, OpenAPI operation extraction/execution; `api_config_routes.py` routes; tests `test_api_connectivity.py` | V2 sample OpenAPI route returns static definition; `pull_definitions` returns static title/count and request keys without fetching/parsing; no operation-test route. | Real HTTP fetch, Swagger/OpenAPI parsing, operation list, parameter/body schema extraction, operation execution, redacted bounded response storage. | High. UI could claim readiness without any real external call, and large/error payloads may leak PHI if naively added later. | Port V1 `api_connectivity` with V2 response models and job/artifact boundaries; persist definition summaries/artifacts; add frontend operation workbench. | Mock HTTP server serves Swagger UI/OpenAPI; V2 pulls it, extracts operations, executes selected operation with required parameters, redacts secrets and caps response preview. |
| Alleva OAuth/API connectivity | V1 `request_client_credentials_token`, token auth styles body/basic, saved credentials usage in `api_connectivity.py` and `api_config_routes.py`; tests for body/basic/saved credentials | V2 has no OAuth token request route/service. | Client-credentials token fetch, auth-style selection, scopes if needed, bearer/API-key headers, redacted token metadata, failure handling. | Critical when live credentials are used. Tokens/secrets must never reach browser/logs; failed live tests must not expose response body. | Port token-request helper and public result shape; keep token in memory only for the operation; redact token URL query and error text; add UI credential-test button. | Mock OAuth server verifies body/basic style and returns token; V2 connectivity test reports safe success metadata only; wrong secret reports safe failure; no token/secret in logs/browser. |
| API test harness jobs/artifacts/progress/cancellation | V1 treatment-plan harness in `alleva_treatment_plan_harness.py`, models, streaming/summary modules, UI route, tests `test_api_treatment_plan_harness*.py`; large-body and error tests | V2 `ApiHarnessJobService` is in-memory only and writes six synthetic rows using `job_artifacts.record`; cancellation is memory flag; no real HTTP, DB persistence, restart recovery, operation selection, or credentials. | Persistent job table, real job runner, HTTP paging, progress, cancellation, artifacts, bounded previews, error/warning logs, restart/stale detection. | High. Synthetic job can falsely prove readiness; in-memory state can lose PHI-sensitive artifacts and audit linkage. | Implement durable job model/service; run jobs against configured API/profile; write JSONL/TSV/CSV/schema/warning/error artifacts incrementally; add cancellation and access control. | Start large mocked Alleva job; initial POST returns under 1s; polling shows progress; cancel stops work; completed job persists across restart and artifacts/preview are bounded/redacted. |
| Treatment-plan sync | V1 `/alleva/treatment-plan-sync/run`, startup sync gating, `alleva_treatment_plan_sync.py`; tests `test_system_and_emr_readiness.py`, `test_treatment_plan_timeliness.py` | V2 has no sync route or service; dashboard says readiness blocked statically. | Gated sync settings, active clients pull, treatment-plans/reviews pull, credential use, status fields, error handling, persistence into normalized tables. | Critical. The app cannot produce real active-client work queues; faking sync risks compliance decisions from synthetic data. | Port V1 sync with V2 gates: disabled by default, requires explicit approval/mapping flags, supports mocked/live read-only runs, records last status and audit events. | With mocked Alleva, admin enables approved test profile, runs sync, records clients/plans/reviews, dashboard updates. Without approval/mapping, sync returns blocker list and does not call external API. |
| Treatment-plan detail pull | V1 detail fetch in `alleva_treatment_plan_sync.py` (`/treatment-plans/{id}`, diagnosis/review detail support) and `/emr/alleva/sync/detail-sample`; tests `test_alleva_detail_sample_fetches_current_plan_content_counts` | V2 has synthetic nested content in `sample_data.py`; no real detail fetch or storage. | Detail endpoint mapping, per-plan detail fetch limit, merge list/detail payloads, content counts/items/signatures/observed fields, failure/skipped statuses. | High. Completeness analysis will miss nested evidence or silently rely on fabricated content. | Port detail fetch/merge and redacted content snapshot logic into V2 aggregate store; expose detail status and raw field explorer. | Mock treatment-plan list plus detail endpoint; V2 stores counts/content items/signature metadata, marks detail fetched, and UI shows source paths without base64 or names. |
| Patient/client matching | V1 matching/ClientId behavior in `alleva_treatment_plan_sync.py`, aggregate builder in `alleva_treatment_plan_aggregate.py`, tests around LUIN/client aliases/name fallback | V2 synthetic record has one fixed `patient_id=307`; no matching logic. | ClientId query behavior, alias/id index, name fallback opt-in, ambiguity warnings, discharge conflict handling, patient-name import/display off by default. | High. Wrong patient-plan alignment can cause incorrect compliance action and PHI exposure. | Port V1 patient-ID-first matching and V2-safe aggregate diagnostics; default name import/fallback off; persist join confidence/warnings. | Mock clients/plans using `ClientId`; matched patient has high-confidence join; ambiguous name fallback is rejected unless explicitly enabled and still records warning. |
| Treatment-plan aggregate construction | V1 `timeliness.treatment_plan_aggregate_payload`, `alleva_treatment_plan_aggregate.py`, frontend types/components; tests `test_alleva_treatment_plan_aggregate.py` and timeliness tests | V2 has Pydantic aggregate schema and one synthetic fixture from `sample_data.py`; no DB/live/manual aggregate builder. | Aggregate builder from normalized DB records, nested content, raw source refs, data quality warnings, evidence coverage, content counts, manager reviews/overrides. | High. Current UI can display convincing but fabricated evidence. | Build V2 aggregate service from persisted clients/plans/reviews/content/criterion reviews; keep schema useful but source data real; source mode explicit. | After mocked sync or manual upload, `/api/v2/treatment-plans` lists real persisted item and detail endpoint returns aggregate with nested content, source evidence, warnings, and no synthetic fixture dependency. |
| Checklist/rules evaluation | V1 `treatment_plan_checklist.py`, `rules_engine.py`, `timeliness.build_checklist_results`, canonical checklist; tests `test_treatment_plan_checklist.py`, `test_rules_engine.py` | V2 loads canonical checklist in `sample_data.py` to create synthetic 42 results; no deterministic evaluation from content. | Evaluation service using all available plan content, missing/conflict/unable states, rationale, evidence refs, recommended actions, workflow profile support. | High. Management may approve incomplete plans if results are fixture-derived. | Port checklist/rules evaluation into V2 over aggregate content; keep LLM disabled and non-required; include source paths and missing fields per criterion. | Fixture with nested intervention content passes relevant criteria; fixture missing signatures/content returns Missing Data/Needs Review; all 42 results include rationale/evidence/missing fields. |
| Timeliness/due-date logic | V1 `timeliness.py` evaluation, dashboard/detail payloads, due-date boundaries, configurable intervals; tests `test_treatment_plan_timeliness.py` | V2 aggregate has hardcoded due dates/status from `sample_data.py`; dashboard metrics are static. | Initial/master/ongoing review windows, PHP/IOP/OP rules, evaluation date support, conflict handling, status selection, active-only queue. | High. Timeliness decisions are compliance-sensitive and cannot be guessed. | Port V1 timeliness evaluator with V2 settings and aggregate output; queue filters query evaluated persisted clients. | Boundary tests for 30/60 day windows pass; missing admission/LOC/signature/source dates produce deterministic Missing Data/Unable/Conflicting states, not guessed compliance. |
| Level-of-care change handling | V1 LOC history model, `treatment_plan_loc_change_window_days`, validated flag, warnings in `timeliness.py`, docs/open blockers | V2 readiness and Settings static text mention unvalidated 7 days; no persisted setting or evaluation path. | Persisted configurable LOC window, validated flag UI, docs blocker, evaluation warnings, no hard-coded final answer. | High. LOC-change rule is explicitly unvalidated and could create false compliance findings. | Keep window configurable/unvalidated; wire into settings/evaluator/readiness; show blocker in UI/docs/reports until resolved. | Changing LOC window in Settings changes evaluation; when validation flag false, readiness/UI/detail show blocker and checklist uses Needs Review/Unable where appropriate. |
| Manual upload/parsing | V1 `/patient-note-sets/*` routes in `routes.py`, `patient_notes.py`, encrypted storage, metadata detection, sync into timeliness; tests `test_patient_note_uploads.py`, `test_security_hardening.py` | Partially restored. V2 Manual Upload now supports protected normalized aggregate JSON import and protected UTF-8 text, Markdown, CSV, TSV, readable PDF, and XLSX uploads with labeled treatment-plan fields. Parsed files build nested V2 aggregates, canonical 42-step checklist evidence, encrypted persisted payloads, encrypted original source-file archives under local app data, redacted audit metadata, visible queue/detail records, safe source-file metadata in detail, and manager/admin source-file download/delete controls. PDF extraction uses bundled `pypdf`; XLSX extraction uses bounded standard-library OpenXML parsing for labeled cells. | Multi-document binder handling, richer patient ID detection/correction UI, source-readiness review state, and broader upload lifecycle audit flows remain open. | Critical. Manual fallback is product-critical; the restored parser, encrypted source archival, audited source download, and audited source delete reduce fake-readiness and plaintext-retention risk, but unsupported formats and scanned/non-extractable PDFs must stay visibly blocked. | Continue the V2 aggregate path: add patient correction/review UI, binder handling, and broader upload lifecycle controls. Keep unsupported/excessive files rejected safely and keep original filenames out of persisted metadata/audit details. | Current proof: synthetic text, TSV, readable PDF, and XLSX uploads post through `/api/v2/manual-uploads/treatment-plan-file`, persist encrypted aggregates, archive original source bytes encrypted without storing original filenames, populate queue/detail, render parsed nested content/checklist evidence, expose source-file metadata, download original bytes through role-gated audited routes with generated `.txt`, `.pdf`, and `.xlsx` filenames, and delete archived source bytes/metadata through manager/admin controls while preserving the aggregate. Remaining proof: binder uploads and patient correction flows pass safely. |
| Audit logging/forensic logs | V1 `AuditLog` model and `audit.py` hash chain, request context, redaction, `/audit/logs`; tests `test_forensic_audit_logging.py` | V2 `backend/app/services/audit.py` writes JSONL fallback with shallow redaction/hash chain; Forensic Logs page is static. No DB query endpoint. | DB audit records, request context, user actor, auth failures/403s, settings/API/sync/upload/review/export events, query UI, deep redaction, hash-chain verification. | Critical. Static logs and shallow redaction do not support forensic review and may miss PHI/secrets. | Port/adapt V1 audit service to V2 DB with hash chain and fallback; expose paged `/api/audit/logs`; UI queries real data; deep-redact nested structures and free text. | Login failure, forbidden admin access, settings save, sync job, upload, manager action, and export create queryable audit rows with actor/request IDs, hash/prev_hash, and no PHI/secrets. |
| Manager overrides/reviews/return actions | V1 timeliness overrides/reviews routes `/timeliness/clients/{id}/overrides`, `/criterion-reviews`; chart workflow transitions; models `TreatmentPlanOverride`, `TreatmentPlanCriterionReview`; tests `test_treatment_plan_timeliness.py`, `test_chart_audit_flow.py` | Partially restored. V2 now persists manager actions in SQLite, validates override reason, role-gates manager/admin actions, creates audit rows, merges persisted reviews/overrides into treatment-plan detail, and displays persisted action history in the UI after reload/restart. | Full return-for-correction workflow/routing, counselor-facing correction queue, richer criterion review states, and failed authorization audit coverage remain to be completed. | Medium/high. Review decisions now persist, but unfinished return/correction workflow can still leave operational accountability gaps. | Extend V2 review/action tables into a full workflow: correction queue, state transitions, role-specific views, failed-authorization audit, and reporting/export integration. | Current proof: manager saves override, detail reload and app restart show action, audit row exists, and browser UI shows persisted action history. Remaining proof: counselor override gets 403/audit failure and return action appears in counselor queue. |
| Export/download behavior | V1 CSV/JSON exports in frontend, document download route with auth/path checks; `patient_notes.resolve_storage_path`; tests `test_patient_note_uploads.py`, frontend export test | Partially restored. V2 checklist evidence export is auth/manager-gated, audited, uses `csv.writer`, neutralizes spreadsheet-formula prefixes in text cells, and was driven over HTTP. API harness artifact downloads now resolve only known generated artifact names, and unknown artifact IDs return 404. Manual source-file downloads and deletion for supported upload formats are manager/admin-gated, audited, path-safe under local app data, and use safe metadata rather than original upload names. | Additional export scopes/formats, failed-download audit events, richer PHI redaction policy, and broader non-admin export denial tests remain open. | Medium/high. Current checklist export, API harness artifact, manual source download, and manual source delete path risks are reduced, but broader download/export lifecycle coverage is incomplete. | Extend export/download coverage across future uploaded documents and sync artifacts; preserve path-safe manifest/DB resolution, role checks, audit, CSV/spreadsheet hardening, and explicit source lifecycle controls for every export/source class. | Current proof: CSV with commas/newlines/quotes parses correctly, formula-like cells are neutralized, valid artifact downloads over HTTP, unknown artifact IDs return 404, direct traversal-style service lookup raises `FileNotFoundError`, archived manual source-file download returns original text/PDF/XLSX bytes only through manager/admin routes with generated filename and audit row, and archived manual source-file delete removes encrypted bytes/metadata while preserving the aggregate and writing an audit row. Remaining proof: broader export scopes are path-safe, redacted, role-gated, and audited. |
| Frontend/backend API integration | V1 monolith `frontend/src/App.tsx` has `apiRequest`, Authorization headers, session storage, real page data fetches; V1 frontend tests cover backend-call contracts | V2 pages import static `data.ts` or render static cards. `frontend/src/v2` has no `fetch` calls; login does not call backend. | API client, session provider, route/page loaders, loading/error/empty states, role-aware nav, mutation flows, download handling. | High. UI currently represents a visual shell and can mislead operators into thinking actions work. | Create V2 frontend API layer; wire every actionable page to backend; disable buttons with precise reason until backend route exists; remove fixture production paths into explicit demo mode. | Frontend tests mock backend and verify login, nav, settings save/reload, users CRUD, API job polling, treatment queue/detail, manager actions, upload, and audit logs call real APIs. |
| Offline demo/synthetic mode isolation | V1 used synthetic fixtures in tests/docs only; V2 objective allows explicit offline demo mode only | V2 production path is built around `syntheticTreatmentPlan`, `dashboardMetrics`, and backend `sample_data.py`; sourceMode type only allows `synthetic_fixture` in frontend. | Explicit demo toggle, disabled by default, labels/watermarks, no production queue from fixtures, grep gate. | High. Synthetic data masquerading as app data is the core beta failure. | Move fixture into `demo` service/UI path gated by config/role; default production APIs query DB/live/mock sync data; type sourceMode supports real modes. | Grep/check finds no production page importing fixture data; app default with empty DB shows empty state; enabling demo mode shows clearly labeled demo record only. |
| API/readiness dashboard | V1 `/system/readiness`, runtime checks, EMR profile/status, treatment checklist readiness; tests `test_system_and_emr_readiness.py`, `test_treatment_plan_checklist.py` | V2 `/api/readiness` has three static checks; Dashboard page static cards. | Real DB/settings/upload encryption/checklist/rules/frontend build/API profile/sync blocker checks, role-aware dashboard metrics. | Medium/high. Operators cannot distinguish configured, blocked, failed, and demo-only states. | Port V1 runtime checks into V2 readiness and dashboard; expose actionable blockers and last job/sync status. | With missing DB/API profile/settings, readiness identifies exact blocker; after setup, status improves; LOC blocker remains warn until validated. |
| Workflow profiles/definitions | V1 workflow definition models/routes/services and UI integration; tests `test_workflow_definitions.py` | V2 `/api/workflow-definitions` returns one static published dict; no CRUD/version/publish/archive. | V2 model/routes/UI for workflow profiles, versioning, publish/archive/delete, validation, default seed. | Medium/high. Configurable workflow behavior from V1 is lost and static route overclaims publish status. | Port V1 workflow definition service into V2 or explicitly supersede with simpler settings-backed profiles; persist and audit. | Admin/manager can create draft, publish version, archive, duplicate keys blocked; published profile affects checklist/timeliness context. |
| Installer/local desktop launch | V1 Windows scripts and packaging under `scripts/`, V2 `desktop_main.py`, `Build-IZ-Windows-Installer.cmd`, `scripts/startup-windows-local.ps1`; tests/smoke scripts | V2 launcher and packaging exist, but runtime it launches is incomplete/fake; docs claim SQLite/encrypted storage/RBAC that active V2 lacks. | Ensure launch initializes DB/bootstrap, serves built frontend, validates no forbidden files, runs real tests, no admin/Docker/Postgres required. | Medium. Packaging can distribute a misleading or insecure shell. | Keep current launch path but wire it to real V2 backend; add smoke checks for auth-protected routes, DB persistence, settings/API config, upload encryption, and fixture isolation. | Double-click/source smoke starts app; unauth route checks fail as expected; release build scans out `.env`, DBs, uploads, logs, node_modules, secrets, and generated PHI-like artifacts. |
| Documentation/versioning | V1/version docs plus current `README.md`, `docs/v2-beta/*`, `VERSION*`, `frontend/package.json` | Docs currently overclaim V2 has SQLite, encrypted uploads/API config, RBAC, real audit, and validation; active code does not. Version metadata is consistent for V2 beta. | Truthful docs for actual state, setup, admin bootstrap, Alleva config/test, API harness, sync, manual upload, review, tests, limitations, validation live-vs-mocked. | High. Overclaiming compliance/security features is operationally dangerous. | After each implementation station, update README, V2 docs, Windows/admin/API/sync/manual upload guides, validation report, known limitations, release notes, and version if release scope changes. | Documentation review grep confirms no unsupported completion claims; validation report states exact commands, live vs mocked Alleva status, remaining blockers, and changed files. |

## Initial Implementation Sequence

1. Stabilize the foundation: DB/session/bootstrap, auth/session/RBAC, users, settings, secure storage, audit.
2. Wire the frontend to real backend APIs and isolate demo data behind an explicit disabled-by-default mode.
3. Restore API configuration, EMR profiles, OAuth/OpenAPI operation testing, and durable API harness jobs.
4. Restore treatment-plan sync/detail pull, patient matching, aggregate construction, checklist/rules, timeliness/LOC handling.
5. Restore manual upload, manager actions, exports/downloads, forensic logs, readiness, workflow profiles as needed.
6. Update docs and run backend, frontend, E2E, grep, packaging, and real-or-mocked Alleva validation gates.

## Current Non-Code Blockers

- The LOC-change treatment-plan update window remains unvalidated by R3/Marleigh and must stay configurable and visibly marked unresolved.
- Live Alleva success cannot be claimed until the app performs safe read-only calls with the provided credentials or documents the exact live blocker; mocked Alleva validation must be clearly labeled as mocked.
