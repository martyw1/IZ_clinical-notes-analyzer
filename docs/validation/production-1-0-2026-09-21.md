# Production 1.0 validation — September 21, 2026

Verified core package: `1.0.0`, build `2026.09.21.2`, installer revision `1`, channel `stable-local-desktop`. The V2 implementation, local-client deployment mode, and schema 12 remain in use. This document records observed evidence; version metadata does not itself certify deployment readiness.

## Scope and discovered defects

The beta build was paused at the user's request. Work continued toward a distinct production 1.0 package with machine-independent first-use administration, forced password change and recovery, portable extraction, and safe forensic diagnostics.

- OneDrive-backed source folders were rejected by installed/data-root reparse checks. A read-only source staging boundary now validates every manifest member before dispatch; destination and ownership checks remain strict.
- Authentication returned distinct inactive/locked failures, repeated a bootstrap-completion event, and local recovery did not invalidate the previous recovery code atomically with its audit. The updated flow uses generic login failures and safe detailed administrator diagnostics.
- Alleva jobs lacked consistent correlation and request-stage context; completion audit included updated external plan IDs. New diagnostics record safe categories, counts, durations, attempts, HTTP statuses, and job IDs.
- Complete client reconciliation could change generated unlinked-plan placeholders to missing patients. The repair excludes only exact generated placeholder keys without a vendor patient identity; genuine historical missing patients remain preserved. The final accepted data contains six such historical vendor identities and nine true unlinked placeholders. Linked plan persistence also preserves inactive and discharged client states instead of overwriting them as active.
- The live verifier omitted exact plan-version/record selectors and mixed source identities. It now follows the product UI's explicit selection contract, preserves intentional HTTP 409 ambiguity protection, and uses Windows PowerShell 5.1-compatible Python transport.

## Evidence recorded before final packaging

| Check | Observed result |
| --- | --- |
| Authentication/recovery focused tests | 33 passed |
| Safe sync/audit/privacy tests | 58 passed; distinct job/persistence tests 7 passed |
| Production identity and fallback | 2 passed |
| UI session tests | 14 passed; TypeScript check passed |
| Complete frontend tests and production build | 182 tests passed across 28 files; Vite build passed |
| Strict destination path tests | 50 assertions passed |
| Production version bridge | 23 assertions passed |
| Source staging, OneDrive, integrity, links and cleanup | 13 assertions passed using the preserved historical package payload |
| Packaging renderer/wrappers and safety | Passed after production identity changes |
| Live OAuth and protected endpoint preflight | Passed; bounded read-only clients and treatment-plan requests each returned HTTP 200 |
| Baseline operational pull | Completed; 401 client observations and 591 plan observations; follow-up checks exposed the placeholder defect above |
| Corrected live workflow | Final accepted run passed: 407 retained patients (401 current vendor clients plus six historical missing identities), 642 retained plan versions (633 linked, nine unlinked), 5 patient and 10 plan detail checks; 459 encrypted snapshots; all linkage, ordering, encryption and privacy invariants passed |
| Live forensic correlation | Start/completion share one job ID; 591 unchanged plans and zero duplicate writes; current-format hash chain valid across 403 events, with 33,443 earlier-format events outside that verifier |
| Direct live plan-detail fetch | Plan detail and diagnoses returned HTTP 200; one selected plan, content retained only in process memory |
| Repeat-run failure diagnosis | A 10-second treatment-plan ReadTimeout was classified safely. Timeout/network GET failures now use the existing three-attempt budget; six focused retry tests passed. The existing local connection timeout was raised to 30 seconds without changing other public settings. |
| Final live acceptance | Passed on the final sync implementation: 401 clients, 591 current vendor plans, 642 retained local plan versions; 591 unchanged and zero duplicate writes; zero errors/warnings; all workflow invariants passed. The current-format hash chain verified 432 events. |
| Approved local administrator reset | Passed after a fresh authenticated encrypted database/environment backup: starter login 200, old password/session 401, workspace 403 pending required change; counts across 45 clinical tables preserved. |
| Initial complete backend run | 615 passed and one intermittent repair shutdown failure; exact process had exited but returned RUNTIME_STOP_FAILED. The process disappeared after its identity check but before Stop-Process acquired it. A narrow catch now accepts this only after confirming the PID is absent; five consecutive complete launch/stop suites passed. The subsequent complete build passed as recorded below. |

The live test uses the existing authorized local tenant configuration. Vendor operations are token acquisition and read-only GET requests. Record contents, credentials and tokens remain local; portable evidence contains counts and fixed diagnostic categories only. Retained historical plans can make local roster totals greater than one current vendor pull.

## Superseded first archive

Build `2026.09.21.1` passed the complete build (616 backend and 182 frontend tests, all seven build gates) at source `cc57961aa0dcddea2fcd3a32124ffd31438b1f53`. Its immutable ZIP hash is `32d891be999148149027f60c620bd74d1ec5c6efd44960d9c95e477ccb2f1ff9`. The exact archive passed the nine-step beta.3 upgrade lifecycle after correcting a beta-only E2E receipt validator. It also passed all 13 source staging/OneDrive checks.

Fresh-profile acceptance exposed a Windows staging-path failure: a 263-character destination for a bundled historical guide image failed in the .NET file copy. This archive is retained as rejected qualification evidence and must not be sent to the client. Build `2026.09.21.2` carries the focused repair and passed the new complete build and exact-archive acceptance below. A direct isolated source-controller installation with the repaired staged-file I/O has already committed successfully using the preserved runtime: starter login succeeded, required password change was true, and workspace access returned 403 until change. The synthetic fixture was removed after a transient shortcut lock released.

Fresh installations now default to the validated 30-second API timeout. A focused HTTP/restart test and legacy-settings suite passed (19 tests), confirming explicitly saved timeout values remain unchanged.

The production-only frontend dependency audit reported zero vulnerabilities. npm reported eight development-dependency warnings (three moderate, five high); development dependencies and Node.js are not part of the client runtime.

## Final production package evidence

**Core acceptance passed on September 21, 2026.** The immutable package is `IZ-Clinical-Notes-Analyzer-v1.0.0-build-2026.09.21.2-installer-r1.zip` (39,220,133 bytes).

- Source revision: `f5d32d30a2aa88d48776694545dd7ba48351c3af` (clean at build).
- ZIP SHA-256: `91d08cd08d01dfff121a3823fd6a62cb93b91ef33e7d1ab7f3adc8cf78be17ec`.
- Build receipt SHA-256: `32336a9997f6c1b21cf658892915166f21c4a06af447587adda96f6df14c9c50`.
- Manifest SHA-256: `1f2ce188a985ac8c5ca221634cbc97191c23685c1570e63951a4c5713b309730`.
- Normal build, no skip flags: **617 backend + 182 frontend tests passed**. All seven gates passed: backend tests, frontend tests, frontend build, repository safety, directory safety, ZIP safety and frozen-bundle inspection. Evidence: `.omo/evidence/production-1-0/full-build-4-result.json` and sibling logs; completed 18:06:45 UTC.
- Exact ZIP P02 lifecycle: **nine steps passed**, covering beta.3 upgrade, executable/HTTP/Edge use, API roundtrip, data-preserving uninstall/reinstall, password rotation and typed purge. Receipt: `.omo/evidence/windows-cmd-maintenance/cmd-c4dcc5bdf8fe/maintenance-run-receipt.json`; case SHA-256 `6954228022ae435c9b4ea6e48b396b04ed98553dca50b729692fdd533b3cde97`. This is component-tier evidence, not a default-profile or actual-client installation claim.
- Two independent fresh component profiles passed starter login, workspace denial before change, forced password change, recovery-code setup/save, rejection of the starter after change, new-password login after a complete managed runtime restart, Settings timeout 30, Help/version identity and visible verified forensic logs. Actual EXE, HTTP and Edge/Playwright were used. Receipt: `.omo/evidence/production-1-0-fresh-profile-qa/runs/cmd-62e30b5c1f6e/fresh-profile-qa-receipt.json`; both synthetic roots purged; zero browser page errors.
- Final source portability checks: **13 assertions passed**, including normal/OneDrive staging, relocated payload verification, tamper/file-set/internal-link rejection and owned cleanup. Evidence: `.omo/evidence/production-1-0/final-package-portability.json`. Focused installer regression also verified a 268-character staging path and rejected an injected junction while preserving its outside sentinel.
- All 340 ZIP entries passed CRC verification. Bootstrap username/password constants were verified in the frozen package; no local `.env` was packaged. Evidence: `.omo/evidence/production-1-0/final-zip-password-verification.json`.
- The final ZIP, detached receipts, checksum and `Production-1.0-START-HERE.txt` were copied to the original workspace's `dist/windows-release` directory. The destination size and SHA-256 match. Evidence: `.omo/evidence/production-1-0/client-copy-verification.json`.

The full build used `VITEST_MAX_WORKERS=2` on this 8 GB Windows host. An earlier attempt at the identical source passed all backend tests but one frontend selected-state assertion failed under default concurrency; the unchanged test passed in isolation and the unchanged complete suite passed with two workers. No assertion or product source was weakened. Early fresh-profile QA attempts also exposed helper-only screenshot-mask and managed-restart observation defects; those ignored helpers were corrected, and the complete two-profile run above passed against the unchanged ZIP.

All 18 fresh-profile screenshots were directly inspected by the parent and two independent reviewers. Capture integrity and cross-profile login consistency passed; the latter had zero differing pixels and is not a comparison against an external design target. Independent functional/DOM and visual/content results are retained in `.omo/evidence/production-1-0/final-ui-review.json`.

The final live sync implementation was exercised with the approved local tenant before packaging; exact-archive lifecycle tests used synthetic data. These are distinct evidence scopes. Final count reconciliation is recorded in `.omo/evidence/production-1-0/live-count-reconciliation.json`; earlier intermediate 401/624/18 local counts are superseded by the final 407/633/9 retained patient/linked/unlinked counts above.

Fresh installs use `admin` and the supplied temporary starter password, then require a personal password and recovery-code setup. Each fresh installation creates its own password hash. Ordinary upgrades/reinstalls preserve the existing password; moving a ZIP does not reset an existing account. The explicitly authorized reset of this laptop is separately verified above. Historical September 15 receipts do not qualify this new archive. Later documentation-only commits do not change the ZIP or its source-bound receipt.

## Cleanup and local backup boundary

Owned test fixtures and test runtimes were removed/stopped; the source candidate and old beta runtime were stopped, with ports 8000 and 8765 free at verification. The local reset was protected by a verified encrypted database plus matching environment snapshot, not a complete profile backup. Full-profile backup in the Codex MSIX environment was blocked by logical/physical local-app-data identity mismatch; no global identity guard was weakened.

Automatic approval review rejected removal of three local temporary backup-staging directories as **blocked by policy**. They remain on this computer, are excluded from the ZIP, and no bypass was attempted. Details remain in local backup evidence; this cleanup limitation is separate from package acceptance.

## External limits

The client's Windows laptop is not connected to this task. Local testing cannot certify its result. Windows 10 and the broader Windows Home/power-loss matrix remain unverified. The release checklist also requires confirmation of credential rotation and R3's signing and retention/legal-hold decisions. No signing certificate or organizational approval is implied by the 1.0 version number. Live startup sync stays disabled, and fresh installations still require authorized tenant configuration.

Default LOC-change timing remains configurable and unvalidated. Existing locally validated settings are preserved; no clinical rule is guessed or finalized by this release.
