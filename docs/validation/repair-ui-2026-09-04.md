# Repair and UI cleanup validation, 2026-09-04

Scope: repair concrete failures found in the current checkout, simplify the existing UI, exercise actual user workflows with synthetic data, and synchronize verified source changes to main. This is source-checkout validation; the prepared Windows installer was not rebuilt.

## S0: Baseline

- Starting branch: main, HEAD e756b67. One pre-existing untracked historical smoke report was preserved.
- Frontend baseline: 173 tests in 26 files passed; production build passed.
- Complete isolated Edge baseline: 24 of 29 browser scenarios passed. Five failures involved shared synthetic fixture assumptions in metrics, roster and selection scenarios. The runner completed, stopped its owned processes and removed the owned synthetic data directory.
- The Git Bash wrapper returned a timeout while its child continued. Subsequent long-running commands use resumable exec sessions; duplicate owned pytest processes from the timeout recovery were stopped. No user app/browser processes were targeted.

## S1: Repairs and UI cleanup

- Dashboard initial-load errors removed every refresh control; failed refreshes also removed the last successful metrics. Two regression tests reproduced those failures before the fix.
- Keep the heading and refresh action mounted, disable refresh during loading, clear the error on retry, and retain previous metrics with an explicit stale-data notice after failed refresh.
- Show risk metrics first, use semantic definition lists, and put counting details in a native disclosure. Clinical counting rules are unchanged.
- Replace the large version heading with the product identity, show a readable role, use compact navigation tabs with mobile horizontal scrolling, and add a visible-on-focus skip link. Preserve every authorized destination and visible version footer.
- Reuse the documented palette, spacing and type tokens. Add minimum control heights, keyboard focus for links/disclosures, and reduced-motion handling.
- Make browser scenarios account for earlier legitimate imports in their shared synthetic runtime. Assertions continue to verify exact selected/historical versions, source/facility restrictions, independent metric deltas, and export contents.
- No application files were deleted. No destructive cleanup or clinical rule changes were made.

## S2: Current verification

- Frontend: 175 tests in 26 files pass; TypeScript noEmit and production build exit 0.
- New installed-Edge UI scenarios: 2/2 pass. Fifteen surfaces captured at 375, 768 and 1280 pixels: login; all eleven admin destinations; populated selected plan; populated selected patient; counselor corrections. Every document fits its viewport. The keyboard skip link moves focus to main content.
- The dashboard browser scenario intentionally supplies HTTP 503 for both an initial load and later refresh. Real backend requests recover successfully; previous counts remain visible during the failed refresh.
- Existing TypeScript LSP is unavailable because installation was previously declined. The repository TypeScript compiler provides the static check.
- Backend: **492/492 pass**, exit 0, 13 existing warnings, 782.86 seconds. Backend source and tests are unchanged. Warnings are Starlette TestClient/httpx deprecation and pytest record_property/xunit2 compatibility. Combined browser and independent visual results are recorded below.

## Evidence and boundaries

Local generated evidence is under `.omo/evidence/repair-ui-20260904-baseline`, `.omo/evidence/repair-ui-20260904-final`, and `.omo/evidence/repair-ui-20260904-combined`. Screenshots deliberately mask inputs; magenta areas are privacy overlays. These artifacts are not release contents.

No live vendor patient data, production credentials, personal browser profile or installed user runtime was used. Live Alleva approval/mapping and the configurable unvalidated LOC-change window remain existing operational blockers. Optional LLM behavior remains disabled by default.

The Astra model guide was read from the official OpenAI documentation. Work used targeted regressions and a finite final verification pass; no model/API configuration was changed.

## Closeout evidence

- Complete shared-runtime Edge run: **31/31 pass**, outer runner exit 0. This includes all existing scenarios and the two new UI/recovery scenarios, after the last rendered-source change.
- Receipt: `.omo/evidence/repair-ui-20260904-verified/all-all-msedge-5e3d7791-82e8-42d8-988f-d2548aedcbf6/playwright-results.json`.
- Baseline progression was 24/29, then 30/31, then 31/31. Each rerun addressed a specific newly confirmed test-isolation failure; successful frontend unit/type checks were not repeatedly rerun for test-only changes.
- The initial visual review identified an orphaned Help tab at desktop width; reducing navigation padding to the existing 8px token resolves it in the final build.
- Final generated assets: `index-B04b70ro.css` and `index-CI3QneXQ.js`. No runtime dependency was added.
- Local/remote work: source inspection, edits, compiler/tests and isolated Edge ran on this Windows device. Remote access was limited to reading the requested official OpenAI guide and Git origin synchronization. OpenAI models performed diagnosis, implementation reasoning and independent review using the returned code and synthetic evidence.
- Backend pytest RAM sample during execution: about 139 MiB across its parent/worker. Peak process RAM and model-host RAM were not exposed.
- Final independent visual review: PASS on the current build. All eleven desktop navigation items fit in one row at 1280px; mobile horizontal navigation remains usable. All 45 manifest captures are present and fit their viewport.
- The adjusted role-label assertion was also verified with the isolated harness: 3/3 pass, including the exact office-manager role value.
- Final browser surface receipts report zero unhandled page errors and zero attempted external requests. Teardown confirms every owned browser/runtime process stopped and its synthetic data directory was removed.
- S0, S1 and S2 are complete. S3 source synchronization follows these passed gates; the sole pre-existing untracked historical smoke report remains untouched.
- Estimated interaction footprint: approximately 200-300 task/tool message exchanges and 150k-250k tokens of analyzed/generated working context including independent agents. These are rough estimates, not metered or billed token totals; cache replays and model-host memory are not exposed.
- Application commit: `b66a901`; browser-isolation commit: `1a466d3`. Synchronization target: local `main` and `origin/main`; final remote SHA confirmation is recorded in the task response.
- Git automatic maintenance emitted permission warnings for old worktree metadata. Fetch/commit operations succeeded; subsequent Git operations disabled automatic maintenance for that invocation only. No old worktree metadata was deleted or repaired as part of this application task.
- Temporary debug journal/JUnit were removed. No backend pytest or owned smoke runtime remains; synthetic screenshot/receipt artifacts stay local under ignored evidence directories.
